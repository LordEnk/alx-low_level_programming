#include "main.h"

/**
 * string_toupper - main block
 *
 *Return: string
 */
char *string_toupper(char *)
{
	int i = 0;
	char str[];

	while (str[i] >= 'a' && str[i] <= 'z')
	{
		str[i] -= 32
			i++
	}
	return (str);
